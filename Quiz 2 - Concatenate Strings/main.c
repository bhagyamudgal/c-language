#include <stdio.h>
#include <string.h>
int main()
{
    char str1[30], str2[30];
    char str3[]=" is friend of ";
    printf("Enter str1 : \n");
    gets(str1);
    printf("Enter str2 : \n");
    gets(str2);
    puts(str1);
    puts(str2);
    puts(str3);
    puts(strcat(str4,str2));
    

    return 0;
}